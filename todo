lions.sh

	line X: Add a flow control to also allow fastq input files
	into tophat and skip the bam extraction steps

	line 80: Control script for Cluster/Local runs (Qsub)

initializeLIONS.sh

	Add R to /bin directory

	Add scripts to convert RepeatMasker.ucsc to ForChimericSearch_hg19,
	SINES_LINES_LTRS_hg19 ...

RNAseqPipeline.sh
	
	Add a protocol to change the wig file to UCSC compatible wig files
	I should have conversion scripts on the gsc somewhere

parameter.ctrl

	line 63: Implement Guided (ab initio) assembly, make a GTF file in resource which will be used to guide assembly
